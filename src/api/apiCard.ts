export let aaa = 1;
/*
import {setTimeout} from "timers";
import {ProductType} from "../app/productsReducer";

export const productFunc = (pro: Array<ProductType>) => {
    console.log(pro[0].price);
}

export const api = {

    /!*getProductsApi() {
        return name.on('value', (snapshot) => {
            snapshot.val()
        });
        console.log(db);
    },*!/
    addProductApi() {
        return new Promise((resolve =>
                setTimeout(() => {
                    resolve(['count'])
                }, 200)

        ))
    }
}
*/
